{
  "name": "star-freight-tycoon",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc && vite build",
    "check": "npm run typecheck && npm run test && npm run build",
    "preview": "vite preview",
    "optimize-assets": "node scripts/optimize-images.mjs"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "sharp": "^0.34.5",
    "typescript": "~5.9.3",
    "vite": "^8.0.0",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
